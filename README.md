# scout-research-assistant
AI-powered research assistant that takes a natural language query, autonomously plans and executes parallel web searches, and synthesizes the results into a structured markdown report — built with a LangGraph backend and React frontend.
