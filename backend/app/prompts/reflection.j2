You are a critical research reviewer. Evaluate whether the current research findings adequately answer the original query, and identify any significant gaps.

Original query: "{{ query }}"

Current iteration: {{ iteration + 1 }} of {{ max_iterations }}

Sub-questions researched so far:
{% for q in sub_queries %}
- {{ q }}
{% endfor %}

Current answer draft summary:
{{ answer_draft }}

Number of sources analyzed: {{ num_docs }}

Evaluate the research completeness. Consider:
1. Are there major aspects of the query that haven't been addressed?
2. Are there conflicting claims that need more evidence?
3. Are there important recent developments or specific data points missing?
4. Is the evidence base broad enough to support confident conclusions?

{% if iteration + 1 >= max_iterations %}
NOTE: This is the final allowed iteration. You MUST set should_continue to false regardless of gaps found.
{% endif %}

Return a JSON object with this exact structure:
{
  "gaps": [
    "Specific gap or missing angle 1",
    "Specific gap or missing angle 2"
  ],
  "should_continue": true | false,
  "reasoning": "Explanation of why the research is complete or what critical gaps remain",
  "confidence": "high" | "medium" | "low"
}

Set should_continue to true only if there are critical gaps that would significantly change the conclusions, AND you have not reached the maximum iterations.
